services:
  ollama:
    image: ollama/ollama:latest
    container_name: azimutai-ollama
    ports:
      - "${OLLAMA_PORT:-11435}:11434"
    volumes:
      - ollama_data:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      - OLLAMA_KEEP_ALIVE=-1
    healthcheck:
      test: ["CMD-SHELL", "ollama list || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 15
      start_period: 5s
    restart: unless-stopped

  ollama-init:
    image: ollama/ollama:latest
    container_name: azimutai-ollama-init
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      - OLLAMA_HOST=http://ollama:11434
    volumes:
      - ./agent-starter-python/Modelfile:/Modelfile:ro
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        echo "Verificando disponibilidad de Ollama..."
        until ollama list > /dev/null 2>&1; do sleep 1; done
        if ! ollama list | grep -q "nexus"; then
          echo "Descargando modelo base llama3.2:3b..."
          ollama pull llama3.2:3b
          echo "Creando modelo personalizado nexus (4096 tokens de contexto)..."
          ollama create nexus -f /Modelfile
          echo "Modelo nexus listo para su uso."
        else
          echo "Modelo nexus ya disponible en el volumen persistente."
        fi
        echo "Precargando modelo nexus en memoria..."
        ollama run nexus "hola" > /dev/null 2>&1
        echo "Nexus activo en memoria y listo para llamadas."
    restart: "no"

  kokoro-tts:
    image: ghcr.io/remsky/kokoro-fastapi-gpu:latest
    container_name: azimutai-kokoro
    ports:
      - "8880:8880"
    volumes:
      - kokoro_cache:/root/.cache
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped

  voice-agent:
    build:
      context: ./agent-starter-python
      dockerfile: Dockerfile
    container_name: azimutai-voice-agent
    depends_on:
      ollama-init:
        condition: service_completed_successfully
      kokoro-tts:
        condition: service_started
    volumes:
      - nemotron_cache:/app/.cache/huggingface
    environment:
      - LIVEKIT_URL=${LIVEKIT_URL}
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET}
      - OLLAMA_BASE_URL=http://ollama:11434/v1
      - KOKORO_BASE_URL=http://kokoro-tts:8880/v1
    restart: unless-stopped

  frontend:
    build:
      context: ./agent-starter-react
      dockerfile: Dockerfile
    container_name: azimutai-frontend
    ports:
      - "3000:3000"
    environment:
      - LIVEKIT_URL=${LIVEKIT_URL}
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET}
      - AGENT_NAME=${AGENT_NAME:-nexus}
      - ALLOW_INSECURE_ROOM_CREATION=true
      - IS_VERCEL_PREVIEW=true
    restart: unless-stopped

volumes:
  ollama_data:
    name: azimutai_ollama_data
  kokoro_cache:
    name: azimutai_kokoro_cache
  nemotron_cache:
    name: azimutai_nemotron_cache
